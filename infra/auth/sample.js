const username = '';
const password = '';
const cluster = '';
const database = '';
const options = '';

export default ConnectionString = `mongodb+srv://${username}:${password}@${cluster}/${database}?${options}`;
