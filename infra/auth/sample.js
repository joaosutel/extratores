const username = '';
const password = '';
const cluster = '';
const database = '';
const options = '';

const ConnectionString = `mongodb+srv://${username}:${password}@${cluster}/${database}?${options}`;

export default ConnectionString;
